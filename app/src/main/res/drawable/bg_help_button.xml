<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple effect for clicks -->
    <item>
        <shape android:shape="rectangle">
            <!-- A very light background color, almost transparent -->
            <solid android:color="@color/backgroundCardSubtle" />
            <corners android:radius="20dp" />
            <!-- A border using your app's primary color -->
            <stroke android:width="1dp" android:color="?attr/colorPrimary" />
        </shape>
    </item>
</ripple>