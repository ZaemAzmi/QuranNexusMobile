<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QuranNexus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>
    <style name="Theme.QuranNexus.NoActionBar"  parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:colorPrimaryDark">@color/black</item>
        <item name="android:listDivider">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <style name="Theme.QuranNexus" parent="Base.Theme.QuranNexus">
        <item name="android:colorPrimaryDark">@color/dark_green_200</item>
    </style>


    <!-- 1. Define the style for the PopupMenu -->
    <style name="Widget.App.PopupMenu" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@drawable/popup_menu_background_dark</item>
    </style>

    <!-- 2. Create a THEME OVERLAY that applies this style -->
    <style name="Theme.App.PopupMenu.Dark" parent="Widget.Material3.PopupMenu">
        <!-- Apply the custom PopupMenu style -->
        <item name="popupMenuStyle">@style/Widget.App.PopupMenu</item>
        <!-- Set the text appearance for the items inside the menu -->
        <item name="android:itemTextAppearance">@style/TextAppearance.App.PopupMenu.Item</item>
    </style>

    <!-- 3. Define the text appearance for the menu items -->
    <style name="TextAppearance.App.PopupMenu.Item" parent="TextAppearance.Material3.BodyLarge">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>