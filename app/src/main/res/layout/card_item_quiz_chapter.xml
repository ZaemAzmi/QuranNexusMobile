<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

    </data>

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/padding_small">

        <TextView
            android:id="@+id/chapterNumberText"
            android:layout_width="@dimen/card_item_chapter_number_width"
            android:layout_height="@dimen/card_item_chapter_number_height"
            android:gravity="center"
            android:background="@drawable/quiz_chapter"
            android:textColor="@android:color/black"
            android:textSize="@dimen/recitation_text_size"
            android:textStyle="bold" />
    </FrameLayout>
</layout>
