<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/nav_header"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:padding="@dimen/padding_medium"
    android:background="@color/light_green_side_nav">

    <ImageView
        android:id="@+id/nav_header_logo"
        android:layout_width="@dimen/nav_header_logo_width"
        android:layout_height="@dimen/nav_header_logo_height"
        android:src="@drawable/quran_nexus_logo" />

    <TextView
        android:id="@+id/nav_header_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-thin"
        android:text="@string/app_name"
        android:textColor="@android:color/black"
        android:textSize="@dimen/text_size_nav_header_title"
        android:textStyle="bold" />

</LinearLayout>